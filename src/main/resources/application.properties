# Exponer endpoint health
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
spring.application.name=UsuariosService

# Configuración de base de datos PostgreSQL para Render
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d3136jumcj7s7380beng-a.oregon-postgres.render.com:5432/usuariosdb_l24y}
spring.datasource.username=${DB_USERNAME:usuariosuser}
spring.datasource.password=${DB_PASSWORD:lqsNGNH1o6YCfhxwHBGgfMWKOjCKVOmn}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración JPA para PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=never
spring.jpa.show-sql=false

server.port=8083

# JWT Configuration
jwt.secret=${JWT_SECRET:mi_clave_secreta_super_segura_para_jwt_que_debe_tener_al_menos_256_bits_para_ser_realmente_segura}
jwt.expiration=${JWT_EXPIRATION:3600}
