# Configuración para producción (Render)
spring.application.name=UsuariosService

# Configuración del servidor
server.port=${PORT:8083}
server.address=0.0.0.0

# Configuración PostgreSQL para Render (usando variables de entorno)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://usuariosdb_l24y_user:ZL8D6JKpZ6sM7J9vVVHrY0ivYUQ0di2D@dpg-d3136jumcj7s7380beng-a:5432/usuariosdb_l24y}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Configuración de producción
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# Endpoints de actuator para health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Configuración de logging
logging.level.root=INFO
logging.level.com.inventario=INFO

# CORS para permitir acceso desde diferentes dominios
cors.allowed-origins=${CORS_ORIGINS:*}
