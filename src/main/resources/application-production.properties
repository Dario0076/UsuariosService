# Configuración para producción (Render) - UsuariosService
spring.application.name=UsuariosService

# Configuración del servidor
server.port=${PORT:8083}
server.address=0.0.0.0

# Configuración PostgreSQL para Render (usando variables de entorno separadas)
spring.datasource.url=jdbc:postgresql://dpg-d3136jumcj7s7380beng-a.oregon-postgres.render.com:5432/usuariosdb_l24y
spring.datasource.username=${DB_USERNAME:usuariosdb_l24y_user}
spring.datasource.password=${DB_PASSWORD:ZL8D6JKpZ6sM7J9vVVHrY0ivYUQ0di2D}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=never
spring.jpa.show-sql=false

# Endpoints de actuator para health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Configuración de logging
logging.level.root=INFO
logging.level.com.inventario=INFO

# CORS para permitir acceso desde diferentes dominios
cors.allowed-origins=${CORS_ORIGINS:*}
